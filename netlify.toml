[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing: send all requests to index.html so Vue Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
